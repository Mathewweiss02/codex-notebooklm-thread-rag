# Experiments

| ID | Hypothesis | Variable | Metric or evidence | Method | Cost | Risk | Dependency | Success signal | Stop condition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| EXP-001 | The current hybrid system meets the release retrieval gate on the full corpus | Query class and decoy density | Semantic candidate recall; hybrid Top-1 | Run 20+ checkpointed varied cases against a dedicated retrieval notebook | Medium | Low | Release-critical fixes | 100% candidate recall and >=95% hybrid Top-1 | Stop on repeated auth/rate-limit failure or any evidence of conversation deletion outside retrieval notebook |
| EXP-002 | Automatic enrollment can remain safe within rolling source headroom | New-task part count and source reserve | Enrolled, deferred-capacity, projected source count | Fixture simulations plus one live new-task enrollment | Medium | Medium | Capacity transaction implementation | No silent omission; atomic config update only when all gates pass | Stop before remote upload if projected capacity exceeds configured budget |
| EXP-003 | A 15-minute quiet gate materially improves freshness without excessive churn | QuietMinutes: 60, 15, 5 | P50/P95 availability lag, revisions/day, uploads/day, failures | Replay update timestamps, then limited live observation | Medium | Medium | Metrics and retention | >=3x freshness improvement with bounded churn and no accuracy regression | Reject a profile if upload volume exceeds budget or failures rise materially |
| EXP-004 | Dedicated retrieval notebooks eliminate CLI history loss without reducing retrieval quality | Shared versus separated conversation topology | CLI conversation preservation and retrieval gate | Automated fixture plus live sacrificial notebook test | Low | Low | Conversation isolation patch | Persistent CLI conversation survives all automated searches | Stop immediately if any non-retrieval conversation is deleted |
| EXP-005 | Retaining current plus one prior projection and bounded reports preserves recovery | Retention count/days | Recoverable revisions, disk usage, stale-file count | Temp-fixture GC tests and guarded live dry run | Low | Medium | Lineage model | Current lineage untouched; superseded data bounded | Refuse apply if any candidate is current, previous, outside root, or unknown |
| EXP-006 | A local router reduces NotebookLM calls at larger scale without lowering candidate recall | Router threshold and shard count | Calls/query, latency, candidate recall | Offline lexical/vector comparison on benchmark cases | Medium | Low | Benchmark corpus | >=50% fewer remote notebook queries with unchanged candidate recall | Drop branch if recall falls below current architecture |
| EXP-007 | Content-addressed verification catches title reuse or silent source drift | Digest verification method | Detected mutations and false positives | Probe upstream source APIs and inject fake drift fixtures | Medium | Low | Upstream capability research | Deterministic drift detection without downloading unrelated content | Stop if upstream cannot expose enough content; retain lineage checks |
| EXP-008 | Locked dependencies are reproducible and have no known critical vulnerabilities | Lock format and audit tool | Clean install, package equality, vulnerability count | Build lock, clean temp runtime, CI audit | Low | Low | None | Reproducible Windows install and zero unresolved critical findings | Stop release on critical vulnerability without mitigation |
| EXP-009 | Streaming oversized records avoids arbitrary line ceilings | Parser strategy | Peak memory, visible-message completeness, runtime | Synthetic 8/32/128 MiB records plus real distribution scan | Medium | Low | Parser fixtures | No visible-message loss and bounded memory | Stop a strategy if it buffers the full record or changes visible-message semantics |
| EXP-010 | Refresh-on-failure or TTL refresh is more reliable than refresh every run | Refresh cadence | Auth failures, refresh calls, run duration | Instrument scheduled runs and simulate expired state | Medium | Medium | Auth truth patch | Fewer refresh calls with no increase in auth failures | Revert on any repeated unattended auth regression |
| EXP-011 | Multi-device ownership rules prevent duplicate or conflicting source lineage | Device concurrency pattern | Duplicate sources, conflicting revisions, recovery time | Two-fixture concurrent sync simulation before live test | High | Medium | Config schema and source identity | One owner per lineage and deterministic failover | Do not run live until fixture conflict tests pass |
| EXP-012 | A governed frozen evaluation suite separates genuine generalization from benchmark memorization | Split, stratum, acceptable-answer set, abstention, holdout exposure | Integrity audit, candidate recall, hybrid Top-1, false positives/negatives, intervals, stability | Freeze the 24-case regression suite; build independently adjudicated development and sealed holdout manifests; hash inputs/labels; preserve raw reports and adjudications | Medium | Low | EXP-001 | Auditable suite plus three frozen runs that can detect regressions and dishonest score movement | Stop on leakage, post-score relabeling, dropped hard cases, unsafe live behavior, or three-round plateau requiring user review |
